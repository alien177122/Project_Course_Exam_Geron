# 🚀 Руководство по запуску проекта Adminka

Этот репозиторий содержит две версии админ-панели:
- **Легаси версия** (корневая папка) - TypeScript + SCSS + ванильный DOM
- **React версия** (папка `project/react-app/`) - React + Vite + Tailwind CSS

## 📋 Предварительные требования

- **Node.js** 18+ 
- **npm** 8+

## 🎯 React версия (Рекомендуется)

### Первый запуск:
```bash
cd project/react-app
npm install
npm run dev
```

Приложение запустится на http://localhost:5173

### Доступные команды:
```bash
npm run dev         # Запуск dev сервера (localhost:5173)
npm run build       # Production сборка
npm run preview     # Предпросмотр production сборки
npm run lint        # ESLint проверка кода
```

### Тестовые данные для входа:
- **Логин:** `user1`
- **Пароль:** `pass123`

### Структура React приложения:
```
src/
├── components/     # Переиспользуемые компоненты
│   ├── ui/        # Базовые UI компоненты (Modal)
│   ├── tariffs/   # Компоненты для тарифов
│   └── Sidebar.tsx
├── hooks/         # React хуки (useAuth, useTariffs)
├── pages/         # Страницы приложения
├── routes/        # Конфигурация TanStack Router
├── types/         # TypeScript интерфейсы
└── utils/         # Утилитарные функции
```

### Что уже реализовано в React версии:
- ✅ Аутентификация с localStorage
- ✅ Управление тарифами (полный CRUD)
- ✅ Модальные окна
- ✅ Типобезопасная навигация (TanStack Router)
- ✅ Tailwind CSS стилизация
- ⏳ Услуги (страница создана, CRUD в планах)
- ⏳ Заказы (страница создана, CRUD в планах)  
- ⏳ Диаграммы доходов (Chart.js интеграция)

## 🏗️ Легаси версия (для справки)

### Первый запуск:
```bash
# В корневой папке проекта
npm install
npm run build
```

### Разработка:
```bash
npm run start       # Запускает watch режим для SCSS и TypeScript
# ИЛИ раздельно:
npm run watch:scss  # Следит за изменениями SCSS
npm run watch:ts    # Следит за изменениями TypeScript
```

### Просмотр:
Откройте `index.html` в браузере или через любой HTTP сервер.

### Доступные команды:
```bash
npm run build:scss    # Компиляция SCSS в CSS
npm run build:ts      # Компиляция TypeScript в JavaScript
npm run watch:scss    # SCSS в watch режиме
npm run watch:ts      # TypeScript в watch режиме
npm run build         # Полная сборка (SCSS + TS)
npm run start         # Запуск обоих watch режимов
```

## 🔧 Разработка

### Добавление новых функций в React версии:
1. Создайте интерфейс в `src/types/index.ts`
2. Создайте хук в `src/hooks/use[Feature].ts` 
3. Создайте компоненты в `src/components/[feature]/`
4. Создайте страницу в `src/pages/[Feature]Page.tsx`
5. Добавьте маршрут в `src/routes/[feature].tsx`
6. TanStack Router автоматически обновит типы при `npm run dev`

### Полезные советы:
- **Горячая перезагрузка:** React версия поддерживает HMR
- **Автогенерация маршрутов:** TanStack Router обновляет `routeTree.gen.ts` автоматически
- **Линтинг:** ESLint настроен для React хуков и TypeScript
- **Стили:** Используйте Tailwind CSS классы

## ⚡ Быстрый старт

Если вы хотите сразу начать разработку:

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd Adminka

# Запустите React версию
cd project/react-app
npm install
npm run dev

# Откройте http://localhost:5173
# Войдите: user1 / pass123
```

## 🐛 Устранение неполадок

### React версия не запускается:
```bash
# Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Ошибки TypeScript в маршрутах:
```bash
# Удалите сгенерированный файл маршрутов
rm src/routeTree.gen.ts
npm run dev  # Файл пересоздастся автоматически
```

### Легаси версия не компилируется:
```bash
# Проверьте версии Node.js и TypeScript
node --version  # Должен быть 18+
npx tsc --version
npm run build
```

---

**Примечание:** React версия находится в активной разработке и является предпочтительной для новых функций. Легаси версия содержит полную бизнес-логику и служит эталоном для миграции.